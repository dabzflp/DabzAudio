/api/key/analyze  https://openkeyscan-analyzer-production.up.railway.app/analyze/single  200
/api/*  https://openkeyscan-analyzer-production.up.railway.app/:splat  200

# Community Hub — proxy the Railway backend under dabzflp.com/community-hub
# This is a 200 rewrite (not a redirect), so the browser address bar stays on dabzflp.com/community-hub.
# Proxy rule MUST come first: Netlify normalizes trailing slashes, so a
# "/community-hub -> /community-hub/" redirect would also catch "/community-hub/"
# and loop. Matching the proxy first means the slashed URL is served directly.
/community-hub/*  https://dabzaudio-production.up.railway.app/:splat  200!
/community-hub    /community-hub/  301!
